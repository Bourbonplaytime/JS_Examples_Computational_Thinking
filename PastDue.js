amount = prompt("Enter the amount owed.");
pastDue = prompt("Enter the days past due");
penalty = 15;


if(amount > 10){
    if(pastDue > 15) {
        console.log(parseInt(amount));
    }
    //console.log(amount);
}